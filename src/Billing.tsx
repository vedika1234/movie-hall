import React, { useContext, useEffect } from "react";

// import { HallContext } from "./Hall";

const Billing = () => {
  // const selectedSeats = React.useContext(HallContext);

  return (
    <>
      {/* {selectedSeats.map((seat: string) => (
        <>seat</>
      ))} */}
    </>
  );
};

export default Billing;
